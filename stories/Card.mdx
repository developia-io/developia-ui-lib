import { Meta, Primary, Controls, Story, Source } from "@storybook/blocks";
import * as CardStories from "./Card.stories";

<Meta of={CardStories} />

# Card

The `Card` component is a versatile container for displaying content. It supports different variants, customizations, and layouts.

<Primary />

## Props

<Controls />

## Stories

### Custom Content Card

This story showcases a `Card` with custom content inside.

<Canvas withSource="open" of={CardStories.CustomContent} />

### Filled Square Card

This story displays a `Card` that is filled and has square corners.

<Canvas withSource="open" of={CardStories.FilledSquare} />

### Card with Image

This example shows a `Card` containing an image.

<Canvas withSource="open" of={CardStories.WithImage} />

### Card with Custom Class

An example of a `Card` using custom classes for styling.

<Canvas withSource="open" of={CardStories.CustomClass} />

### All Props Example

This story demonstrates the use of all props available in the `Card` component.

<Canvas withSource="open" of={CardStories.AllProps} />

### Horizontal Layout Card

A `Card` with a horizontal layout using `flex` properties.

<Canvas withSource="open" of={CardStories.HorizontalLayout} />

### Vertical Layout Card

This story shows a `Card` with a vertical layout.

<Canvas withSource="open" of={CardStories.VerticalLayout} />

### Outlined Card

A simple outlined `Card` example.

<Canvas withSource="open" of={CardStories.Outlined} />

### Transparent Card

An example of a `Card` with a transparent background.

<Canvas withSource="open" of={CardStories.Transparent} />

### Transparent Card with Image

A `Card` that is transparent and includes an image.

<Canvas withSource="open" of={CardStories.TransparentWithImage} />
